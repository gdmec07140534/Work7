apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'gdmec'
            keyPassword 'lcz360424331'
            storeFile file('D:/Program Files/Android/gdmec.jks')
            storePassword 'lcz360424331'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.1"
    defaultConfig {
        applicationId "com.example.henry.work7"
        minSdkVersion 22
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile files('libs/BaiduLBS_Android.jar')
}
